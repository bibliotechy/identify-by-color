{% include 'head.j2' %}

  <body style="padding-top: 0px;">
  {% include 'nav.j2' %}
    <div class="jumbotron">
     <div class="container-fluid">
          <h2> I tried to get that color but, ¯\_(ツ)_/¯ </h2>
      </div>
              <div class="container-fluid">
            <form class="navbar-form navbar-left"  action="/color" role="search">
                <div class="form-group">
                    <input id="color-search" name="color" type="text" class="form-control typeahead"
                            data-provide="typeahead" autocomplete="off"
                            placeholder="{% if name %} {{ name }} {% else %} Try A Different Color? {% endif %}" />
                </div>
            <button type="submit" class="btn btn-default">Submit</button>
      </form>

         </div>
     <div class="container-fluid">
          <h3>Or let us <a href="/rando-colrissian"> pick on for you.</a></h3>
      </div>
    </div>

{% include 'end-body-js.j2' %}
  </body>
</html>